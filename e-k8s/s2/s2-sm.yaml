apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cmpt756s2
  labels:
    app: cmpt756s2
    release: komplete-prometheus
spec:
  namespaceSelector:
    matchNames:
    - cmpt756e4
  selector:
    matchLabels:
      app: cmpt756s2
  endpoints:
  - port: http
    path: /metrics
